{
  "builds": [{ "src": "studybud/wsgi.py", "use": "@vercel/python", "config": { "buildCommand": "pnpm install --frozen-lockfile && pnpm run build && python manage.py collectstatic --noinput" } }],
  "routes": [
    { "src": "/(.*)", "dest": "studybud/wsgi.py" }
  ]
}
