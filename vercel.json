{
  "builds": [{ "src": "studybud/wsgi.py", "use": "@vercel/python", "config": { "buildCommand": "corepack pnpm install --frozen-lockfile && corepack pnpm run build && python manage.py collectstatic --noinput" } }],
  "routes": [
    { "src": "/(.*)", "dest": "studybud/wsgi.py" }
  ]
}
