{
  "name": "studybud",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "description": "StudyBuddy — Django + Tailwind CSS",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./tailwind/input.css -o ./static/css/tailwind.css --minify",
    "watch": "npx @tailwindcss/cli -i ./tailwind/input.css -o ./static/css/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
